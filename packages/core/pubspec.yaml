name: core
description: Ayni SOS domain entities, value objects, and repository interfaces
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  equatable: ^2.0.7
  uuid: ^4.5.1
  meta: ^1.15.0

dev_dependencies:
  test: ^1.25.8
