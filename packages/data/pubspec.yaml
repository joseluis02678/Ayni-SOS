name: data
description: Local SQLite (Drift) and remote API repository implementations
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  sync_engine:
    path: ../sync_engine
  dio: ^5.7.0
  shared_preferences: ^2.3.3
  flutter_secure_storage: ^9.2.4
  uuid: ^4.5.1
  crypto: ^3.0.6
  cross_file: ^0.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
